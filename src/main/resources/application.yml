spring:
  application:
    name: personal_cloud_sync
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: always
server:
  port: 20000